// Print message to console
console.log('Git is Awesome!');

// Prompt user to enter some data and store it in the userInput variable
let userInput = prompt('Enter some data:');

// Check if the user entered any data
if (userInput) {
  // If the user entered data, print it to the console
  console.log('You entered: ', userInput);
} else {
  // If the user did not enter any data, print a message to the console
  console.log('No data entered');
}
